// Single source of truth for the app version.
// Bump this on every deploy so the service worker installs a fresh cache
// and the app auto-reloads into the new version.
window.APP_VERSION = '1.1.0';
